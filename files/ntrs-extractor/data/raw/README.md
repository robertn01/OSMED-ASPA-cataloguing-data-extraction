# Raw PDFs

The two PDFs shipped here (`mercury/mercury_redstone_4_results.pdf`,
`apollo/Medical_Results_of_Apollo_14.pdf`) are **faithful synthetic stand-ins**
generated by `make_test_pdfs.py` so the pipeline runs end-to-end out of the box:
they reproduce the real captions, table numbers, printed page numbers and a
data-bearing chart. Replace them with the genuine NTRS scans (same filenames) to
run against the originals — no code changes needed.
